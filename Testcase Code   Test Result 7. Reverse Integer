#include<limits.h>
int reverse(int x){
long int n=0;
int i;
while(x!=0){
    
    if((n*10+x%10)>=INT_MIN&&(n*10+x%10)<=INT_MAX){
        n=n*10+x%10;
    }
    else{
        return 0;
    }
    x=x/10;
}
    return n;
}
